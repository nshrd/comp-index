# CBMA14 Index - Минимальные зависимости
# Обновлено после анализа фактического использования

# Основные библиотеки
pandas>=2.0.0              # Для обработки данных в builder
requests>=2.31.0           # Для API запросов (Coinglass)
python-dotenv>=1.0.0       # Для переменных окружения

# Flask и веб-сервер
flask>=3.0.0               # UDF API сервер
flask-cors>=4.0.0          # CORS поддержка
gunicorn>=21.2.0           # WSGI сервер для production 